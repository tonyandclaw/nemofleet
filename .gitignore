# ─── secrets / credentials (NEVER commit) ────────────────────────────────────
.env
# full-fleet export bundles — contain EVERY secret (tokens/keys/certs/device cred); never commit
nemofleet-export-*.tar.gz
nemofleet-export-*.tar.gz.gpg
*.pem
*.key
*.crt
*.csr
*.srl
*.p12
*.cred
**/.bridge-token
**/.bridge-token.prev
**/.approval-key
**/.approval-key.prev
**/.nvd-api-key
services/bridge/dash-users.json
services/bridge/dash-auth.json
services/bridge/notified.json
services/bridge/*.json.tmp
config/bridge/dash-seed.json

# ─── python / virtualenvs ────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ─── runtime data (kept as dirs via .gitkeep, contents ignored) ──────────────
data/bus/inbox/*
data/bus/outbox/*
data/logs/*
chats/*
!**/.gitkeep
*.log
services/bridge/jira-tickets/
services/**/jira-store*
stack-sbom.json
cve-scan-history.jsonl

# ─── generated artifacts ─────────────────────────────────────────────────────
tools/design-system/components/

# ─── media kept local-only (per project decision) ────────────────────────────
docs/decks/*
docs/assets/*
*.pptx

# ─── OS / editor noise ───────────────────────────────────────────────────────
.DS_Store
*.swp
.claude/
tests/ui/node_modules/

# nuclei binary + templates cache (fetched by worker-b-install-nuclei.sh)
.cache/

# ENW knowledge — ASUS-internal product design distilled from Confluence; never push to remote
knowledge/enw/

# ─── demo-only (local presenter script; never push) ──────────────────────────
scripts/demo-showcase.sh
